# Copy this file to .env and configure as needed

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Secret (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Database Configuration
# Option 1: PostgreSQL (Recommended for production)
# Uncomment and configure if you want to use PostgreSQL:
# DATABASE_URL=postgresql://username:password@localhost:5432/ai_studio

# Option 2: SQLite (Default, no configuration needed)
# If DATABASE_URL is not set, SQLite will be used automatically
# SQLITE_DB_PATH=./data/ai_studio.db

# File Upload
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

